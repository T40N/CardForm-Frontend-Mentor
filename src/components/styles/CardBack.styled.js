import styled from "styled-components";

const CardBackStyled = styled.div`
  background: url(${require("../../images/bg-card-back.png")}) center / fit
    no-repeat;
  width: 200px;
  height: 80px;
`;

export default CardBackStyled;
