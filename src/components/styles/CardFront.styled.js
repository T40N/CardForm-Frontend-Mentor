import styled from "styled-components";

const CardFrontStyled = styled.div`
  background: url(${require("../../images/bg-card-front.png")}) center / fit
    no-repeat;
  width: 200px;
  height: 80px;
`;

export default CardFrontStyled;
